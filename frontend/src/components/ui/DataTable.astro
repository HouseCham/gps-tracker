---
import type { DataTableColumn } from '@/types/components/ui';

/**
 * @interface Props
 * @property {string | undefined} [caption] - Table caption.
 * @property {DataTableColumn[]} columns - Table columns.
 * @property {boolean} [loading=false] - Show loading indicator.
 * @property {boolean} [empty=false] - Show empty state.
 * @property {string} [emptyTitle='No records found'] - Empty state title.
 * @property {string} [emptyMessage='Nothing to show here yet.'] - Empty state message.
 * @property {string | undefined} [class] - Extra classes.
 */
interface Props {
    caption?: string;
    columns: DataTableColumn[];
    loading?: boolean;
    empty?: boolean;
    emptyTitle?: string;
    emptyMessage?: string;
    class?: string;
}

const {
    caption,
    columns,
    loading = false,
    empty = false,
    emptyTitle = 'No records found',
    emptyMessage = 'Nothing to show here yet.',
    class: className,
} = Astro.props;

const rootClasses = [
    'data-table',
    loading && 'is-loading',
    empty && 'is-empty',
    className,
]
    .filter(Boolean)
    .join(' ');
---

<div class="data-table__scroll">
    <table class={rootClasses}>
        {caption && <caption class="data-table__caption">{caption}</caption>}
        <thead>
            <tr>
                {
                    columns.map((col) => {
                        const align = col.align ?? 'left';
                        return (
                            <th
                                scope="col"
                                style={col.width ? `width: ${col.width}` : undefined}
                                class:list={[
                                    'data-table__head',
                                    col.sortable && 'is-sortable',
                                    `is-align-${align}`,
                                ]}
                                aria-sort={col.sortable ? 'none' : undefined}
                            >
                                {col.sortable ? (
                                    <button class="data-table__sort" type="button">
                                        <span>{col.label}</span>
                                        <svg
                                            class="data-table__sort-icon"
                                            width="10"
                                            height="10"
                                            viewBox="0 0 10 10"
                                            fill="none"
                                            aria-hidden="true"
                                        >
                                            <path d="M5 2l3 4H2l3-4z" fill="currentColor" />
                                        </svg>
                                    </button>
                                ) : (
                                    col.label
                                )}
                            </th>
                        );
                    })
                }
            </tr>
        </thead>
        <tbody>
            {empty ? (
                <tr class="data-table__empty-row">
                    <td class="data-table__cell data-table__empty" colspan={columns.length}>
                        <span class="data-table__empty-icon" aria-hidden="true">
                            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><path d="M8 12h8"/></svg>
                        </span>
                        <span class="data-table__empty-title">{emptyTitle}</span>
                        <span class="data-table__empty-msg">{emptyMessage}</span>
                    </td>
                </tr>
            ) : loading ? (
                Array.from({ length: 5 }).map(() => (
                    <tr class="data-table__row data-table__row--skeleton" aria-hidden="true">
                        {columns.map((col) => (
                            <td class="data-table__cell">
                                <span class:list={['data-table__skeleton', `data-table__skeleton--${col.key}`]} />
                            </td>
                        ))}
                    </tr>
                ))
            ) : (
                <slot />
            )}
        </tbody>
    </table>
</div>

<style>
    .data-table__scroll {
        background: var(--surface);
        border: none;
        border-radius: var(--radius);
        box-shadow: var(--shadow-md);
        overflow: auto;
        max-height: 100%;
    }

    .data-table__caption {
        position: absolute;
        width: 1px;
        height: 1px;
        margin: -1px;
        padding: 0;
        overflow: hidden;
        clip: rect(0, 0, 0, 0);
        white-space: nowrap;
        border: 0;
    }

    .data-table {
        width: 100%;
        border-collapse: separate;
        border-spacing: 0;
        font-size: 13px;
    }

    .data-table thead th {
        position: sticky;
        top: 0;
        z-index: 1;
        background: var(--surface-2);
        color: var(--muted);
        font-weight: 600;
        font-size: 11px;
        text-transform: uppercase;
        letter-spacing: 0.06em;
        text-align: left;
        padding: 10px 16px;
        border-bottom: 1px solid var(--border);
        white-space: nowrap;
    }

    .data-table__head.is-sortable {
        padding: 0;
    }

    .data-table__sort {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        width: 100%;
        height: 100%;
        padding: 10px 16px;
        background: none;
        border: 0;
        color: inherit;
        font: inherit;
        text-transform: inherit;
        letter-spacing: inherit;
        text-align: left;
        cursor: pointer;
        transition: color var(--transition);
    }

    .data-table__sort:hover {
        color: var(--text);
    }

    .data-table__sort-icon {
        opacity: 0.4;
        transition: transform var(--transition), opacity var(--transition);
    }

    .data-table__head.is-sorted-asc {
        color: var(--text);
    }
    .data-table__head.is-sorted-asc .data-table__sort-icon {
        opacity: 1;
        transform: rotate(0deg);
    }
    .data-table__head.is-sorted-desc {
        color: var(--text);
    }
    .data-table__head.is-sorted-desc .data-table__sort-icon {
        opacity: 1;
        transform: rotate(180deg);
    }

    .data-table tbody td.data-table__cell {
        padding: 12px 16px;
        border-bottom: 1px solid var(--border);
        color: var(--text);
        vertical-align: middle;
    }
    .data-table tbody tr:last-child td {
        border-bottom: 0;
    }
    .data-table__row:hover td {
        background: var(--bg);
    }

    .data-table .is-align-right {
        text-align: right;
    }
    .data-table .is-align-center {
        text-align: center;
    }

    /* Loading skeleton */
    .data-table__skeleton {
        display: block;
        height: 12px;
        width: 100%;
        border-radius: 4px;
        background: linear-gradient(
            90deg,
            var(--surface-2) 0%,
            var(--border) 50%,
            var(--surface-2) 100%
        );
        background-size: 200% 100%;
        animation: dt-shimmer 1.4s linear infinite;
    }
    .data-table__skeleton--name {
        width: 60%;
    }
    .data-table__skeleton--email {
        width: 80%;
    }
    .data-table__skeleton--status {
        width: 64px;
        height: 20px;
        border-radius: 9999px;
    }
    .data-table__skeleton--role,
    .data-table__skeleton--actions,
    .data-table__skeleton--time {
        width: 50%;
    }

    @keyframes dt-shimmer {
        0% {
            background-position: 200% 0;
        }
        100% {
            background-position: -200% 0;
        }
    }

    /* Empty state */
    .data-table__empty {
        text-align: center;
        padding: 48px 16px;
        color: var(--muted);
    }
    .data-table__empty-row td {
        border-bottom: 0;
    }
    .data-table__empty {
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 8px;
    }
    .data-table__empty-icon {
        width: 40px;
        height: 40px;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        background: var(--surface-2);
        color: var(--muted);
        border-radius: 50%;
        margin-bottom: 4px;
    }
    .data-table__empty-title {
        color: var(--text);
        font-weight: 600;
        font-size: 14px;
    }
    .data-table__empty-msg {
        font-size: 13px;
        max-width: 36ch;
    }

    @media (max-width: 767.98px) {
        .data-table thead th,
        .data-table__sort,
        .data-table tbody td.data-table__cell {
            padding: 10px 12px;
        }
        .data-table__scroll {
            border-radius: var(--radius-sm);
        }
    }
</style>
