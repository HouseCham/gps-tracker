---
import type { GetStaticPaths } from 'astro';
//-- Translations
import { getTranslation } from '@/i18n';
//-- Types
import type { Language } from '@/types';
//-- Components
import MainLayout from '@/components/astro/layout/MainLayout.astro';
import PageHeader from '@/components/astro/layout/PageHeader.astro';
import { ApiKeyTable } from '@/components/react/api-keys';
/**
 * @type {GetStaticPaths}
 */
export const getStaticPaths = (() => {
    return [{ params: { lan: 'en' } }, { params: { lan: 'es' } }];
}) satisfies GetStaticPaths;

//* note: getStaticPaths only emits the two supported locales,
//   so the cast is exhaustive by construction.
const locale = Astro.params.lan as Language;
const translation = getTranslation(locale);
---

<MainLayout
    locale={locale}
    translation={translation}
    active="api-keys"
    showAdmin={true}
    title={`${translation.apiKeys.title} · GPS Tracker`}
>
    <PageHeader
        eyebrow={translation.apiKeys.title}
        title={translation.apiKeys.title}
    />

    <ApiKeyTable
        client:only="react"
        locale={locale}
        translation={translation}
    />
</MainLayout>